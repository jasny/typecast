<?php

/**
 * Just a test
 * @ignore
 * 
 * @param string $one  One
 * @param string $two  One plus one
 * @return string
 */
function metatest_foo_bar($one, $two)
{
    return "$one + $one = $two";
}
